# Squid HTTP/HTTPS forward proxy — allow by default, blocklist-based blocking.
# Only HTTP-speaking services should point at this (OpenClaw, Trivy, Wazuh HTTP updaters).
# Raw-TCP outbound (SMTP, some feeds) goes via platform-egress directly.

name: egress-proxy

networks:
  platform-net:
    external: true           # clients reach proxy at squid:3128 via this
  platform-egress:
    external: true           # squid's OWN outbound path to real internet

volumes:
  squid-cache:
  squid-logs:
  squid-blocklists:

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  squid:
    build: { context: ., dockerfile: Dockerfile.squid }
    image: platform/squid:6.13-patched
    container_name: egress-proxy
    restart: unless-stopped
    logging: *default-logging
    volumes:
      - ./squid.conf:/etc/squid/squid.conf:ro
      - squid-cache:/var/spool/squid
      - squid-logs:/var/log/squid
      - squid-blocklists:/etc/squid/blocklists
    networks:
      - platform-net         # clients connect here
      - platform-egress      # proxy's own outbound
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - SETUID
      - SETGID
      - DAC_OVERRIDE
    security_opt:
      - no-new-privileges:true
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=1
      - net.ipv6.conf.default.disable_ipv6=1
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    healthcheck:
      # Foreground Squid (-N) may not leave /run/squid.pid for `squid -k check`;
      # verify the running process directly instead.
      test: ["CMD-SHELL", "pidof squid >/dev/null"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  blocklist-updater:
    build: { context: ./blocklist-updater, dockerfile: Dockerfile }
    image: platform/blocklist-updater:1.0
    container_name: blocklist-updater
    restart: unless-stopped
    logging: *default-logging
    volumes:
      - squid-blocklists:/blocklists
    networks:
      - platform-egress       # outbound only — not on platform-net
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD-SHELL", "test -s /blocklists/malicious_ips.txt"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 2m
    deploy:
      resources:
        limits:
          cpus: "0.1"
          memory: 64M
